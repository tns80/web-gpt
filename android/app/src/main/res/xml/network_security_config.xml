<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy: HTTPS only by default. Cleartext is disallowed at the
  application level via android:usesCleartextTraffic="false" in the manifest;
  this config additionally pins the policy and lets you whitelist a development
  host (e.g. 10.0.2.2 for the Android emulator) without flipping the global flag.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Uncomment and edit to allow cleartext traffic to a dev host:
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    -->
</network-security-config>
